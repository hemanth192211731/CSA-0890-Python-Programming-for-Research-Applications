# Most Significant Bit (MSB)
def get_msb(n):
    if n == 0:
        return 0
    msb = 0
    while n > 1:
        n = n >> 1
        msb += 1
    return 1 << msb

number = 18
msb = get_msb(number)
print("Most Significant Bit (MSB):", msb)
