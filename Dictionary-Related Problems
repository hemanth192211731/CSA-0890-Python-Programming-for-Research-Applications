# Dictionary-Related Problems
# Example: Counting occurrences of words in a sentence

sentence = "this is a test this is only a test"
word_counts = {}
words = sentence.split()

for word in words:
    if word in word_counts:
        word_counts[word] += 1
    else:
        word_counts[word] = 1

print("Word occurrences:", word_counts)
